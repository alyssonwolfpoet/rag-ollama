# Database (use asyncpg driver)
DATABASE_URL=postgresql+asyncpg://postgres:ifce@localhost:5432/rag

# Redis (para Celery/locks/cache)
REDIS_URL=redis://localhost:6379/0

# JWT (em dev HS256; em prod prefira RS256)
SECRET_KEY=troque_por_uma_chave_segura_aqui
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Ollama
OLLAMA_BASE_URL=http://localhost:11434
EMBEDDING_MODEL=nomic-embed-text
GENERATION_MODEL=gemma3

# RAG params
CHUNK_SIZE=500
CHUNK_OVERLAP=50
TOP_K=5
VECTOR_DIM=1536
